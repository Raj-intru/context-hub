{
  "name": "lyra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lyra — a safe, multi-model AI workspace for families and schools.",
  "main": "src/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js",
    "test:e2e": "node test/e2e.mjs",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "stripe": "^16.8.0",
    "node-cron": "^3.0.3"
  }
}
